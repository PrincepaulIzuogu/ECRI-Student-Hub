{
  "name": "My Dev Container",

  "dockerComposeFile": ["../docker-compose.yml"],
  "service": "frontend",

  "extensions": ["dbaeumer.vscode-eslint"],

  "settings": {
    "terminal.integrated.shell.linux": "/bin/bash"
  },

  "postCreateCommand": "apk update && apk add curl && curl -fsSL https://get.docker.com -o get-docker.sh && sh get-docker.sh && apk add docker-compose && docker-compose up --build",
  "forwardPorts": [8000, 3000]
}
